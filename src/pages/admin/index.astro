---
import '@/styles/cms.css';
import config from '@/config';
import Layout from '@/layouts/Layout.astro';
---
<Layout title="Admin Page" description='Here we find a place of Force'>
    <!-- Include the script that builds the page and powers StaticJs CMS -->
    <script is:inline src="https://unpkg.com/@staticcms/app@^2.0.0/dist/static-cms-app.js"></script>

    <!-- Netlify Identity Widget -->
    <script is:inline src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <script is:inline define:vars={{ config }}>
      window.CMS.init({ config });
    </script>
</Layout>
